import React from 'react';
import TodosList from "./TodosList";

const Todos = ({}) => {
    return (
        <div>
            {/* @ts-ignore */}
            <TodosList />
        </div>
    );
};

export default Todos;
// by Rokas with ❤️
